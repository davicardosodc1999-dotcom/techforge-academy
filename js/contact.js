document.addEventListener("DOMContentLoaded",()=>{const f=document.getElementById("contact-form"),s=document.getElementById("contact-status"),EMAIL="CLIENT-EMAIL-HERE";if(!f||!s)return;f.addEventListener("submit",e=>{e.preventDefault();if(!f.reportValidity())return;if(EMAIL==="CLIENT-EMAIL-HERE"){s.textContent="Contact form ready. Add the client receiving email before launch.";return}const d=new FormData(f),sub=encodeURIComponent(`[PLATFORM CONTACT] ${d.get("topic")}`),body=encodeURIComponent(`Name: ${d.get("name")}\nEmail: ${d.get("email")}\nTopic: ${d.get("topic")}\n\n${d.get("message")}`);location.href=`mailto:${EMAIL}?subject=${sub}&body=${body}`})});